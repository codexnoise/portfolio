import Link from 'next/link'

const Navbar = () => {
  return (
    <nav>
      <button>Home</button>
      <button>About</button>
      <button>Projects</button>
      <button>Contact</button>
    </nav>
  )
}

export default Navbar
